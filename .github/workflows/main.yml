name: main

on:
  push:
    branches:
    - '**'
    tags:
    - '**'
  pull_request:
    branches:
    - '**'

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Say hi
      run: |
        echo hi
    - name: Secret step
      if: ${{ secrets.mysecret }} != ''
      run: |
        echo Running secrets-only step
  windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1
    - name: Say hi
      run: |
        echo hi
  macos:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v1
    - name: Say hi
      run: |
        echo hi