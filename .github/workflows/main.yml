name: main

on:
  push:
    branches:
    - '*'
    tags:
    - '*'
  pull_request:
    branches:
    - '*'

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Say hi
      run: |
        echo hi
  windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1
    - name: Say hi
      run: |
        echo hi
